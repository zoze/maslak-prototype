# مسلك — منصة النقل المدرسي

## ما هو هذا المشروع

بروتوتايب تصميمي (Design Prototype) لمنصة نقل مدرسي سعودية باسم **مسلك**. المشروع حاليًا **ليس تطبيقًا مبرمجًا** — هو مجموعة ملفات HTML مستقلة قابلة للفتح مباشرة في المتصفح، تُستخدم للعرض والمراجعة مع أصحاب القرار. البيانات كلها توضيحية (hardcoded).

الجهة: جمعية الإحسان — القسم التقني.

## بنية الملفات

```
index.html                      # صفحة الهبوط — فهرس البروتوتايبات الأربعة
maslak-platform-prototype.html  # الواجهة الأم: إشراف مسلك على كل المدارس (6 شاشات)
school-web-prototype.html       # منصة المدرسة: طلاب/أسطول/غياب/تقارير (6 شاشات)
driver-app-prototype.html       # تطبيق السائق: رحلة حية + تحضير الطلاب (6 شاشات)
parent-app-prototype.html       # تطبيق ولي الأمر: تتبع + غياب + تواصل (6 شاشات)
docs/                           # أبحاث وقرارات تصميمية موثّقة
```

كل ملف HTML **مكتفٍ ذاتيًا** (self-contained): CSS و JS مضمّنان بالكامل داخل الملف. الاعتماد الخارجي الوحيد هو خط Tajawal من Google Fonts.

## المعمارية داخل كل ملف

نمط موحّد عبر الملفات الأربعة — لا تكسره:

- **مكدّس شاشات (screen stack)**: مصفوفة `stack` تحتفظ بتاريخ التنقّل. `go(id)` تدفع شاشة، `go(id,{root:true})` تستبدل الجذر (للتبويبات)، `back()` ترجع خطوة.
- **الشاشات**: `<div class="screen" id="s-{name}">` وتُفعَّل بإضافة `.active`.
- **`render()`**: تُظهر الشاشة النشطة، تحدّث حالة التبويبات/الروابط الجانبية، وتضبط `location.hash`.
- **`.proto-bar`**: الشريط الداكن العلوي — أداة مراجعة للمصمّم فقط، **ليست جزءًا من التطبيق**. تُبنى أزراره ديناميكيًا من مصفوفة `SCREENS` وقاموس `LABELS`.
- **قشرة الجوال**: `.phone-shell` (390px) لتطبيقي السائق وولي الأمر.
- **قشرة المتصفح**: `.browser-frame` + `.sidebar` للمنصتين الويب.

## الهوية البصرية

| المتغير | القيمة | الاستخدام |
|---|---|---|
| `--primary` | `#5C4CE3` | البنفسجي الأساسي |
| `--primary-dark` | `#4636bf` | التدرّجات |
| `--primary-light` | `#EDEBFC` | خلفيات ناعمة، أيقونات |
| `--text` | `#16162C` | نص كحلي داكن |
| `--text-muted` | `#6B6B85` | نص ثانوي |
| `--bg` | `#F7F7FB` | خلفية الصفحة |
| `--sky` / `--pink` / `--yellow` / `--lightblue` | `#BEEFF5` / `#FFD1E3` / `#FFEFB0` / `#D6DEFF` | باستيل ثانوية |
| `--green` / `--red` | `#22C55E` / `#EF4444` | نجاح / خطأ |

- الخط: **Tajawal** (400/500/700/800).
- الاتجاه: **RTL** — `dir="rtl"` على `<html>`. استخدم `margin-inline-start/end` لا `left/right`.
- الأزرار: كبسولة (`border-radius: 999px`).
- البطاقات: بيضاء، `border-radius: 20-24px`، ظل ناعم `0 8px 24px rgba(22,22,44,.06)`.
- الأسلوب: Clay ثلاثي الأبعاد مبسّط، أيقونات SVG بخط `stroke-width: 2` و `fill: none`.

## قواعد العمل على هذا المشروع

- **لا تُدخل أي إطار عمل أو أداة بناء** (React، npm، bundler). الملفات تُفتح بالنقر المزدوج — هذه ميزة وليست قيدًا مؤقتًا.
- **لا تنقل CSS إلى ملف مشترك** — كل ملف مستقل عمدًا حتى يمكن إرساله وحده.
- عند إضافة شاشة جديدة: أضف `<div class="screen" id="s-X">`، وأضف `'X'` إلى `SCREENS`، وأضف تسميتها العربية إلى `LABELS` (وإلى `CRUMB` في الواجهتين الويب).
- كل نص واجهة **بالعربية**. لا تترك نصًا إنجليزيًا في واجهة المستخدم.
- المحتوى موجّه للسوق السعودي: أسماء، أحياء رياضية، تواريخ هجرية/ميلادية مناسبة.
- التوثيق في `docs/` بالعربية.

## الفجوات المعروفة (نقطة العمل النشطة)

في [driver-app-prototype.html](driver-app-prototype.html) شاشة `s-roster`: التحضير **يدوي بالكامل** — ✓/✗ لكل طالب من قائمة ثابتة. الفجوات:

- **لا يوجد دور «مرافق»** — وهو المسؤول نظامًا عن الصعود والنزول في حافلات الابتدائي (م ٣٠/١١ من اللائحة التنفيذية لنشاط النقل التعليمي). **أكبر فجوة وظيفية.**
- **لا آلية «خلو الحافلة»** في نهاية المسار — وهي **إلزام نظامي** (م ٢٦/٢١).
- **لا طبقة تصدير لمنصة الهيئة العامة للنقل** — والربط **إلزامي** (م ٣٠/٤).
- لا تحضير عند النزول (المسجَّل هو الصعود فقط).
- لا تقنية تعريف آلي.
- القائمة تعرض كل الطلاب لا طلاب المحطة الحالية.

## قيود قرارية مستقرة

هذه حُسمت بحثًا — لا تُعِد فتحها بلا سبب جديد:

- **لا تعرّف على وجه ولا بصمة.** بيانات حساسة تحت PDPL + قاصرون + خصوصية الطالبات + لا غطاء تنظيمي (اللائحة لا تفرض كاميرات).
- **لا تصوير للطلاب** في أي واجهة. صمّم متطابقًا بين الجنسين **بالبناء لا بالإعداد** — لا وضع «بنين» ووضع «بنات».
- **أي وسم تعريف يحمل رقمًا مجهولًا فقط** — لا بيانات شخصية عليه (نمط Zonar).
- **طبقة السلامة مستقلة تمامًا** عن بيانات التحضير.
- **العمل دون اتصال إلزامي** — المزامنة لاحقة.
- **معيار القبول الأول: زمن المحطة.** أي حل يضيف أكثر من ٣٠ ثانية للمحطة = حل فاشل.

## التوثيق

راجع [docs/README.md](docs/README.md) — وابدأ بـ [docs/04-competitive-landscape.md](docs/04-competitive-landscape.md) فهو يمسّ جدوى المشروع.
